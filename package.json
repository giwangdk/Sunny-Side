{
  "dependencies": {
    "bootstrap": "^4.6.0",
    "jquery": "^3.6.0",
    "node-sass": "^6.0.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass --watch scss -o css"
  }
}
